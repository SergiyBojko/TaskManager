<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_cancel"
        android:title="@string/cancel_button_text"
        app:showAsAction="always"
        app:actionLayout="@layout/cancel_action_view"
        android:background="@color/toolbar_action_selector"/>
    
    <item
        android:id="@+id/menu_save_task"
        android:title="@string/save_button_text"
        app:actionLayout="@layout/save_task_action_view"
        app:showAsAction="always"
        android:background="@color/toolbar_action_selector"/>

</menu>